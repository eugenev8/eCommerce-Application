const ROUTES_PATHS = {
  main: '/',
  login: '/login',
  register: '/register',
  userProfile: '/profile',
  catalog: '/catalog',
  product: '/product',
  basket: '/basket',
  about: '/about',
} as const;

export default ROUTES_PATHS;
